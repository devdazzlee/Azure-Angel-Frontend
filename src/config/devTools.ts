/**
 * Developer Tools Configuration
 *
 * Toggles for debug/testing UI that should never reach real users.
 * Flip these by hand when you need them locally, then flip back before committing.
 */

export const SHOW_SKIP_QUESTION_BUTTON = true;
